# Dépendances
node_modules
.pnp
.pnp.js

# Build Vite
dist
dist-ssr
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Éditeurs / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets locaux — NE JAMAIS COMMITER
.env
.env.local
.env.*.local
supabase-config.local.js

# Claude Code — préférences locales
.claude/settings.local.json

# Supabase CLI (cache local)
supabase/.temp
supabase/.branches

# Encodage HLS : dossiers de travail et journal du worker
tmp-hls/
workers/encoder/worker.log
workers/encoder/launchd.out.log
workers/encoder/launchd.err.log

# Toute variante de fichier d'environnement (sauvegardes, brouillons)
.env*
!.env.example

# Médias de travail lourds — hors dépôt (audit 21/07/2026).
# Livrables clients / exports : n'ont rien à faire dans git (~4 Go de bloat).
# NB : index.html référence Photos_ezla-davy/couple/05.jpg, mais ce fichier
# n'a jamais été suivi et n'existe pas sur disque (lien cassé préexistant,
# les vraies photos sont en .jpeg) — rien d'utilisé n'est donc retiré ici.
/Photos_ezla-davy/
/E&D1.jpeg
/OrnelaLuka.png
/StessyJunior.png
/ThelanaManoah.png
/027.jpeg
